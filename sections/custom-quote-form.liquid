<section id="quote-form" class="quote-section">
  <div class="quote-section__inner">
    <div class="quote-section__head">
      {%- if section.settings.eyebrow != blank -%}<div class="quote-section__eyebrow">{{ section.settings.eyebrow }}</div>{%- endif -%}
      {%- if section.settings.heading != blank -%}<h2 class="quote-section__title">{{ section.settings.heading }}</h2>{%- endif -%}
      {%- if section.settings.text != blank -%}<p class="quote-section__text">{{ section.settings.text }}</p>{%- endif -%}
    </div>

    {%- comment -%} Quote form handled by the AAA Form Builder app (form 16105); restyled below to match the design. {%- endcomment -%}
    <div class="aaa_formbuilder_container" style="width: 100%; margin: 0 auto; float: none;"><form method="post" action="" class="aaaformbuilder_form  aaa_formbuilder_form_id_16105" enctype="multipart/form-data"><input type="hidden" name="url" class="get_current_page_url" value="{{ canonical_url }}"><input type="hidden" name="container_id" value=""><input type="hidden" name="form_id" value="16105"><input type="hidden" name="form_domain" value="aml89.myshopify.com"><input type="hidden" name="" value="Thank You! We'll be in touch" class="success_msg"><input type="hidden" name="" value="Please fill all the required fields" class="error_msg"><input type="hidden" name="" value="Please enter a valid email address." class="emailerror_msg"><input type="hidden" name="" value="" class="aaa_thankyoupageurl"><input type="hidden" name="uploadeLimit" class="formUploadLimit" value="10"><input type="hidden" name="allowefiletype" class="allowedfiletype" value=""><input type="hidden" name="conditionallogic" class="conditionallogic" value=""><div class="fb-text form-group field-first-name"><label for="first-name" class="fb-text-label">Name <span class="required">*</span></label> <input type="text" required="" placeholder="Enter your full name" class="form-control field__input" name="first-name" id="first-name"></div><div class="fb-email form-group field-replyemail"><label for="replyemail" class="fb-email-label">Email <span class="required">*</span></label> <input type="email" required="" placeholder="Enter your email address" class="form-control field__input" name="replyemail" id="replyemail" aria-required="true"></div><div class="fb-number form-group field-number-1703859243649"><label for="number-1703859243649" class="fb-number-label">Phone Number</label> <input type="number" class="form-control field__input" name="number-1703859243649" placeholder="Enter your phone number" id="number-1703859243649"></div><div class="fb-text form-group field-width"><label for="width" class="fb-text-label">Width In Inches</label> <input type="text" placeholder="Enter width" class="form-control field__input" name="width" id="width"></div><div class="fb-text form-group field-height"><label for="height" class="fb-text-label">Height In Inches</label> <input type="text" placeholder="Enter Height" class="form-control field__input" name="height" id="height"></div><div class="fb-file form-group field-file-1704224393638"><label for="file-1704224393638" class="fb-file-label">Upload Reference Image</label> <input type="file" class="form-control" name="file-1704224393638" id="file-1704224393638"></div><div class="fb-undefined form-group field-textarea-1481364899702"><label for="textarea-1481364899702" class="fb-textarea-label">Customize Your Own Mosaic Design <span class="required">*</span></label><textarea required="" placeholder="Give us details about color, size, shape or design" class="form-control field__input" name="textarea-1481364899702" id="textarea-1481364899702"></textarea></div><div class="fb-submit form-group field-button-1481029333721"><div class="aaa_captcha_cont"><label>Captcha</label><span class="rand1">8</span> + <span class="rand2">8</span> = <input type="text" class="aaa_captcha_result" style="width: 60px; display: inline-block;"></div><button type="submit" class="button-input btn button btn-default" name="button-1481029333721" id="button-1481029333721">Submit Request</button></div></form></div>
    <script> function isMyScriptLoaded() { var url = 'https://shopifycdn.aaawebstore.com/formbuilder/appfiles/aaa_form_builder_script_minify_v1.js?shop=aml89.myshopify.com'; var jsFound = 0; var scripts = document.getElementsByTagName('script');  for (var i = scripts.length; i--;) { if (scripts[i].src == url) { jsFound = 1; } } if(!jsFound) { var headTag = document.getElementsByTagName('head')[0]; var jqTag = document.createElement('script'); jqTag.type = 'text/javascript'; jqTag.src = url; jqTag.onload = ''; headTag.appendChild(jqTag); } } isMyScriptLoaded(); </script>
  </div>
</section>

<style>
  .quote-section { background: var(--color-navy); color: #EAE1D2; scroll-margin-top: 90px; }
  .quote-section__inner { max-width: 760px; margin: 0 auto; padding: 80px 40px; }
  .quote-section__head { text-align: center; margin-bottom: 42px; }
  .quote-section__eyebrow { font-family: var(--font-accent); font-size: 12px; letter-spacing: .3em; text-transform: uppercase; color: var(--color-blue-light); margin-bottom: 14px; }
  .quote-section__title { font-family: var(--font-display); font-weight: 500; font-size: clamp(30px, 3.4vw, 46px); line-height: 1.1; margin-bottom: 14px; color: #fff; }
  .quote-section__text { font-size: 16px; line-height: 1.7; color: var(--color-text-faint); margin: 0 auto; max-width: 520px; }
  .quote-form__field { margin-bottom: 20px; }
  .quote-form__row { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
  .quote-form__label { display: block; font-size: 11px; letter-spacing: .14em; text-transform: uppercase; color: var(--color-text-faint); margin-bottom: 9px; }
  .quote-form__req { color: var(--color-blue-light); }
  .quote-form__input { width: 100%; background: rgba(255,255,255,.06); border: 1px solid rgba(220,230,245,.22); outline: none; color: #fff; font-family: var(--font-body); font-size: 15px; padding: 14px 16px; }
  .quote-form__input::placeholder { color: var(--color-text-faint); }
  .quote-form__textarea { resize: vertical; }
  .quote-upload { position: relative; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; gap: 8px; padding: 30px 20px; cursor: pointer; background: rgba(255,255,255,.04); border: 1.5px dashed rgba(220,230,245,.3); transition: border-color .2s ease, background .2s ease; }
  .quote-upload:hover, .quote-upload.is-drag { border-color: var(--color-blue-light); background: rgba(43,123,232,.08); }
  .quote-upload__input { position: absolute; inset: 0; width: 100%; height: 100%; opacity: 0; cursor: pointer; }
  .quote-upload__icon { color: var(--color-blue-light); display: flex; }
  .quote-upload__title { font-size: 14.5px; color: #EAE1D2; }
  .quote-upload.has-file .quote-upload__title { color: #fff; font-weight: 600; }
  .quote-upload__hint { font-size: 12px; color: var(--color-text-faint); }
  .quote-form__submit { background: var(--color-blue); color: #fff; border: none; font-family: var(--font-body); font-size: 13px; font-weight: 600; letter-spacing: .14em; text-transform: uppercase; padding: 16px 44px; cursor: pointer; transition: background .25s ease; margin-top: 6px; }
  .quote-form__submit:hover { background: #1E63C4; }
  .quote-form__error { font-size: 13px; color: #F2B8B8; margin-bottom: 18px; }
  .quote-form__success { text-align: center; padding: 24px 0 10px; }
  .quote-form__success-icon { width: 68px; height: 68px; border-radius: 50%; background: rgba(43,123,232,.18); display: flex; align-items: center; justify-content: center; margin: 0 auto 24px; }
  .quote-form__success-title { font-family: var(--font-display); font-weight: 500; font-size: 30px; margin-bottom: 12px; color: #fff; }
  .quote-form__success-text { font-size: 16px; line-height: 1.65; color: var(--color-text-faint); max-width: 380px; margin: 0 auto; }
  @media (max-width: 600px) { .quote-form__row { grid-template-columns: 1fr; } }

  /* AAA Form Builder app form — restyled to match the design */
  .aaa_formbuilder_container { background: transparent !important; width: 100% !important; }
  .aaaformbuilder_form .form-group { margin-bottom: 20px; box-sizing: border-box; }
  .aaaformbuilder_form label { display: block; font-family: var(--font-body); font-size: 11px; letter-spacing: .14em; text-transform: uppercase; color: var(--color-text-faint) !important; margin-bottom: 9px; font-weight: 400; }
  .aaaformbuilder_form .required { color: var(--color-blue-light) !important; }
  .aaaformbuilder_form .form-control { width: 100%; box-sizing: border-box; background: rgba(255,255,255,.06); border: 1px solid rgba(220,230,245,.22); border-radius: 0; outline: none; box-shadow: none; color: #fff; font-family: var(--font-body); font-size: 15px; padding: 14px 16px; }
  .aaaformbuilder_form .form-control:focus { border-color: var(--color-blue-light); }
  .aaaformbuilder_form .form-control::placeholder { color: var(--color-text-faint); }
  .aaaformbuilder_form textarea.form-control { min-height: 120px; resize: vertical; }
  .aaaformbuilder_form .fb-file .form-control { padding: 12px 16px; color: var(--color-text-faint); }
  .aaaformbuilder_form .field-width, .aaaformbuilder_form .field-height { display: inline-block; width: calc(50% - 9px); vertical-align: top; box-sizing: border-box; }
  .aaaformbuilder_form .field-width { margin-right: 16px; }
  .aaaformbuilder_form .aaa_captcha_cont { margin-bottom: 18px; color: var(--color-text-faint); font-size: 13px; letter-spacing: .08em; }
  .aaaformbuilder_form .aaa_captcha_cont label { display: inline; margin-right: 10px; text-transform: uppercase; }
  .aaaformbuilder_form .aaa_captcha_result { background: rgba(255,255,255,.06); border: 1px solid rgba(220,230,245,.22); color: #fff; padding: 8px 10px; }
  .aaa_formbuilder_container .aaaformbuilder_form .fb-submit button { background: var(--color-blue) !important; color: #fff !important; border: none !important; border-radius: 0 !important; font-family: var(--font-body) !important; font-size: 13px !important; font-weight: 600 !important; letter-spacing: .14em !important; text-transform: uppercase !important; padding: 16px 44px !important; width: auto !important; cursor: pointer !important; margin-top: 6px; }
  .aaa_formbuilder_container .aaaformbuilder_form .fb-submit button:hover { background: #1E63C4 !important; }
  @media (max-width: 600px) { .aaaformbuilder_form .field-width, .aaaformbuilder_form .field-height { display: block; width: 100%; margin-right: 0; } }
</style>

<script>
  (function () {
    var box = document.querySelector('[data-upload]');
    if (!box) return;
    var input = box.querySelector('[data-upload-input]');
    var title = box.querySelector('[data-upload-title]');
    var placeholder = title.textContent;
    function show() {
      if (input.files && input.files.length) {
        title.textContent = input.files[0].name;
        box.classList.add('has-file');
      } else {
        title.textContent = placeholder;
        box.classList.remove('has-file');
      }
    }
    input.addEventListener('change', show);
    ['dragenter', 'dragover'].forEach(function (ev) {
      box.addEventListener(ev, function (e) { e.preventDefault(); box.classList.add('is-drag'); });
    });
    ['dragleave', 'drop'].forEach(function (ev) {
      box.addEventListener(ev, function (e) { e.preventDefault(); box.classList.remove('is-drag'); });
    });
    box.addEventListener('drop', function (e) {
      if (e.dataTransfer && e.dataTransfer.files.length) { input.files = e.dataTransfer.files; show(); }
    });
  })();
</script>

{% schema %}
{
  "name": "Custom quote form",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Make It Yours" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Request a Custom Quote" },
    { "type": "textarea", "id": "text", "label": "Text", "default": "Share your dimensions or your own image and our artisans will hand-cut a one-of-a-kind mosaic, tailored to your space." }
  ],
  "presets": [{ "name": "Custom quote form" }]
}
{% endschema %}
