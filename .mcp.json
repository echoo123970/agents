{
  "mcpServers": {
    "Shopify": {
      "type": "http",
      "url": "https://${SHOPIFY_STORE_DOMAIN}/api/mcp",
      "headers": {
        "X-Shopify-Access-Token": "${SHOPIFY_ACCESS_TOKEN}"
      }
    }
  }
}
